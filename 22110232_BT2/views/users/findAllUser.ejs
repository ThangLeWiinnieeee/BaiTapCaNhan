<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Users - CRUD</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .table-hover tbody tr:hover {
            background-color: rgba(0,0,0,.075);
        }
        .action-links a {
            margin: 0 5px;
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <h2 class="mb-4">Danh Sách Users</h2>
        
        <div class="mb-3">
            <a href="/crud" class="btn btn-primary">Tạo User Mới</a>
            <a href="/home" class="btn btn-secondary ms-2">Trang Chủ</a>
        </div>
        
        <div class="table-responsive">
            <table class="table table-striped table-hover table-bordered">
                <thead class="table-dark">
                    <tr>
                        <th>Email</th>
                        <th>First Name</th>
                        <th>Last Name</th>
                        <th>Address</th>
                        <th>Phone Number</th>
                        <th>Gender</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (datalist && datalist.length > 0) { %>
                        <% for(let i = 0; i < datalist.length; i++) { %>
                            <tr>
                                <td><%= datalist[i].email %></td>
                                <td><%= datalist[i].firstName %></td>
                                <td><%= datalist[i].lastName %></td>
                                <td><%= datalist[i].address || 'N/A' %></td>
                                <td><%= datalist[i].phoneNumber || 'N/A' %></td>
                                <td>
                                    <% if (datalist[i].gender === true) { %>
                                        Female
                                    <% } else { %>
                                        Male
                                    <% } %>
                                </td>
                                <td class="action-links">
                                    <a href="/edit-crud?id=<%= datalist[i]._id %>" class="btn btn-warning btn-sm">Edit</a>
                                    <a href="/delete-crud?id=<%= datalist[i]._id %>" 
                                       class="btn btn-danger btn-sm" 
                                       onclick="return confirm('Bạn có chắc chắn muốn xóa user này?')">Delete</a>
                                </td>
                            </tr>
                        <% } %>
                    <% } else { %>
                        <tr>
                            <td colspan="7" class="text-center text-muted">No users found</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
        
        <div class="mt-3">
            <p class="text-muted">Total Users: <strong><%= datalist ? datalist.length : 0 %></strong></p>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>